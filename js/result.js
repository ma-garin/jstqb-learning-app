import{STORAGE_KEYS,readJson}from'./storage.js';const result=readJson(STORAGE_KEYS.result,null);if(result){document.getElementById('score').textContent=`${result.correct} / ${result.total}`;document.getElementById('score-message').textContent=result.total?`正答率 ${Math.round(result.correct/result.total*100)}%`:'問題はありませんでした。'}
