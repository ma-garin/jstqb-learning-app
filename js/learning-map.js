import{topics}from'./content.js';const list=document.getElementById('topic-list'),empty=document.getElementById('topic-empty');empty.hidden=topics.length>0;topics.forEach(topic=>{const article=document.createElement('article');article.className='topic';const heading=document.createElement('h2');heading.textContent=topic.title;const description=document.createElement('p');description.textContent=topic.description;article.append(heading,description);list.append(article)});
