import{STORAGE_KEYS,readJson,writeJson,recordAnswered}from'./storage.js';
const session=readJson(STORAGE_KEYS.session,null),empty=document.getElementById('quiz-empty'),content=document.getElementById('quiz-content');let selected=null;
function finish(){writeJson(STORAGE_KEYS.result,{correct:session.correct,total:session.questions.length});localStorage.removeItem(STORAGE_KEYS.session);location.href='result.html'}
function render(){if(!session?.questions?.length){empty.hidden=false;return}if(session.index>=session.questions.length){finish();return}content.hidden=false;const question=session.questions[session.index];document.getElementById('progress-label').textContent=`${session.index+1}/${session.questions.length}`;document.getElementById('question-topic').textContent=question.topic||'問題';document.getElementById('question-text').textContent=question.question;const choices=document.getElementById('choices');choices.textContent='';question.choices.forEach((text,index)=>{const label=document.createElement('label');label.className='choice';const input=document.createElement('input');input.type='radio';input.name='answer';input.value=String(index);input.addEventListener('change',()=>{selected=index});label.append(input,document.createTextNode(text));choices.append(label)});document.getElementById('feedback').hidden=true;document.getElementById('answer-button').hidden=false;selected=null}
document.getElementById('answer-button').addEventListener('click',()=>{if(selected===null)return;const question=session.questions[session.index],correct=selected===question.correctAnswerIndex;if(correct)session.correct++;recordAnswered(question.id);document.querySelectorAll('input[name=answer]').forEach(input=>input.disabled=true);document.getElementById('feedback-title').textContent=correct?'正解です':'不正解です';document.getElementById('explanation').textContent=question.explanation;document.getElementById('feedback').hidden=false;document.getElementById('answer-button').hidden=true;writeJson(STORAGE_KEYS.session,session)});document.getElementById('next-button').addEventListener('click',()=>{session.index++;writeJson(STORAGE_KEYS.session,session);render()});render();
