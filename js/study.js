import{questions}from'./content.js';import{STORAGE_KEYS,writeJson}from'./storage.js';
export function shuffleItems(items,random=Math.random){const result=[...items];for(let i=result.length-1;i>0;i--){const j=Math.floor(random()*(i+1));[result[i],result[j]]=[result[j],result[i]]}return result}
const summary=document.getElementById('question-summary'),empty=document.getElementById('study-empty'),start=document.getElementById('start-button'),limit=document.getElementById('question-limit'),shuffle=document.getElementById('shuffle');
if(summary&&empty&&start&&limit&&shuffle){summary.textContent=`登録済みの独自問題: ${questions.length}問`;empty.hidden=questions.length>0;start.disabled=questions.length===0;start.addEventListener('click',()=>{let selected=shuffle.checked?shuffleItems(questions):[...questions];if(limit.value!=='all')selected=selected.slice(0,Number(limit.value));writeJson(STORAGE_KEYS.session,{questions:selected,index:0,correct:0});location.href='quiz.html'})}
